// index.js
const head = require('./head');
const middle = require('./middle');
const without = require('./without');
const findKey = requre('./findKey');
const takeUntil = require('./takeUntil');
const countOnly = requre('./countOnly');


module.exports = {
    head: head,
    middle: middle,
    without: without,
    findKey: findKey,
    takeUntil: takeUntil,
    countOnly: countOnly

};
